using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExitDoors : MonoBehaviour
{
    /*[SerializeField] public bool isAtWaterDoor = false;
    [SerializeField] public bool isAtFireDoor = false;

    private void OnTriggerEnter2D(Collider2D collision)
    {

        if (collision.tag == "Player1" && gameObject.name == "Fire exit")
            isAtFireDoor = true;
        if (collision.tag == "Player2" && gameObject.name == "Water exit")
            isAtWaterDoor = true;
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        if (collision.tag == "Player1" && gameObject.name == "Fire exit")
            isAtFireDoor = false;
        if (collision.tag == "Player2" && gameObject.name == "Water exit")
            isAtWaterDoor = false;
    }

    public bool getIsAtDoor()
    {
        return isAtWaterDoor;
    }*/
}
